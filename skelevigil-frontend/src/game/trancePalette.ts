/**
 * Trance dual-plane palette helpers will live here (teal diamond plane + amber hex plane),
 * parallel to `glimpsePalette.ts` and `starePalette.ts`. Add builders when Trance gameplay ships.
 */
export const TRANCE_PALETTE_MODULE_RESERVED = true;
