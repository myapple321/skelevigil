rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Default deny; update matches for user assets.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}

